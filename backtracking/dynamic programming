def fib(n):
    dp = [0, 1]
    for i in range(2, n+1):
        dp.append(dp[i-1] + dp[i-2])
    return dp[n]

print(fib(6))  # Output: 8

Dynamic Programming is an optimization technique used to avoid solving the same subproblem multiple times.
