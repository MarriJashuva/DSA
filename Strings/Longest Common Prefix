list=["flower", "flow", "flight"]
list=sorted(list)
str1=list[0]
str2=list[len(list)-1]
str=""
for i in range(0,len(str1)+1):
    if str1[i]!=str2[i]:
        break
    else:
        str+=str1[i]
print(str)
        
